CREATE TABLE PAIS (ID_PAIS NUMBER PRIMARY KEY, NOME VARCHAR2 (30));

DROP TABLE PAIS;

CREATE TABLE ESTADO(ID_ESTADO NUMBER PRIMARY KEY, NOME VARCHAR2 (20), ID_PAIS NUMBER);

ALTER TABLE ESTADO ADD CONSTRAINT FK_ESTADO FOREIGN KEY (ID_PAIS) REFERENCES PAIS (ID_PAIS);

CREATE TABLE CIDADE (ID_CIDADE NUMBER PRIMARY KEY, NOME VARCHAR (20), ID_ESTADO NUMBER);

ALTER TABLE CIDADE ADD CONSTRAINT FK_CIDADE FOREIGN KEY (ID_ESTADO) REFERENCES ESTADO (ID_ESTADO);

CREATE TABLE BAIRRO(ID_BAIRRO NUMBER PRIMARY KEY, NOME VARCHAR2 (20), ID_CIDADE NUMBER);

ALTER TABLE BAIRRO ADD CONSTRAINT FK_BAIRRO FOREIGN KEY (ID_CIDADE) REFERENCES CIDADE (ID_CIDADE);

CREATE TABLE ENDERECO_CLI(ID_CLIENTE NUMBER PRIMARY KEY, NOME VARCHAR2 (100), CEP VARCHAR2 (12), ID_BAIRRO NUMBER);

ALTER TABLE ENDERECO_CLI ADD CONSTRAINT FK_END_CLIENTE FOREIGN KEY(ID_BAIRRO) REFERENCES BAIRRO(ID_BAIRRO);

INSERT INTO ENDERECO_CLI (ID_CLIENTE, NOME, CEP, ID_BAIRRO) VALUES (1, 'LUANA', '09345678', 1); --> ERRO POIS NÃO HÁ DADOS EM BAIRRO

INSERT INTO PAIS VALUES(1, 'BRASIL');

INSERT INTO ESTADO VALUES(1,'SAO PAULO',1);
INSERT INTO CIDADE  VALUES(1,'SAO PAULO',1);
INSERT INTO BAIRRO VALUES(1,'BELA VISTA',1);
INSERT INTO ENDERECO_CLI VALUES(1, 'THIEMI', '09345678', 1);

SELECT * FROM ENDERECO_CLI;
